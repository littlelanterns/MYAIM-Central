# Production environment configuration
# This file ensures builds succeed on deployment platforms

# Disable ESLint warnings during build
DISABLE_ESLINT_PLUGIN=true

# Don't treat warnings as errors in CI/CD
CI=false

# Generate sourcemaps for debugging (optional - set to false for smaller builds)
GENERATE_SOURCEMAP=true
