import React from 'react';

interface ToastProps {
  // TODO: Define props for Toast
}

const Toast: React.FC<ToastProps> = (props) => {
  return (
    <div className="-----">
      {/* TODO: Implement Toast */}
    </div>
  );
};

export default Toast;
