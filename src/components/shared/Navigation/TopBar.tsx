import React from 'react';

interface TopBarProps {
  // TODO: Define props for TopBar
}

const TopBar: React.FC<TopBarProps> = (props) => {
  return (
    <nav className="------">
      {/* TODO: Implement TopBar */}
    </nav>
  );
};

export default TopBar;
